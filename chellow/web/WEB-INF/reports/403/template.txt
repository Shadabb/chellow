<!doctype html>
<html>
  <head>
    <link rel="stylesheet" type="text/css"
      href="{{context_path}}/reports/19/output/">
    <title>Chellow &gt; Gas Supplier Contracts &gt; {{g_contract.name}}</title>
  </head>
  <body>
    <p>
      <a href="{{context_path}}/reports/1/output/">Chellow</a> &gt;
      <a href="{{context_path}}/reports/395/output/">Gas Supplier Contracts</a>
      &gt;
      <a href="{{context_path}}/reports/399/output/?g_contract_id={{g_contract.id}}">{{g_contract.name}}</a>
      &gt; Edit
    </p>
    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
      </ul>
    {% endif %}
    {% if request.getMethod() == 'GET' and request.getParameter('delete') %}
      <form method="post" action=".">
        <fieldset>
          <legend>
            Are you sure you want to delete this contract?
          </legend>
          <input type="hidden" name="g_contract_id" value="{{g_contract.id}}">
          <input type="submit" name="delete" value="Delete">
        </fieldset>
      </form>
      <p>
        <a href=".">Cancel</a>
      </p>
    {% else %}
      <form action="." method="post">
        <fieldset>
          <legend>Update Contract</legend>
          <input type="hidden" name="g_contract_id" value="{{g_contract.id}}">
          <br>
          <label>Name</label>
          {{input_text('name', g_contract.name)}}
          <br>
          <br>
          Charge script
          <br>
          {{input_textarea('charge_script', g_contract.charge_script, 40, 80)}}
          <br>
          <br>
          Properties
          <br>
          {{input_textarea('properties', g_contract.properties, 20, 80)}}
          <br />
          <br />
          <input type="submit" name="update" value="Update">
        </fieldset>
      </form>
      <br />
      <form action=".">
        <fieldset>
          <legend>Delete this contract</legend>
          <input type="hidden" name="g_contract_id" value="{{g_contract.id}}">
          <input type="submit" name="delete" value="Delete">
        </fieldset>
      </form>
    {% endif %}
  </body>
</html>

<!doctype html>
<html>
  <head>
    <link rel="stylesheet" type="text/css"
      href="{{context_path}}/reports/19/output/">
    <title>Chellow &gt; Gas Supplier Contracts</title>
  </head>
  <body>
    <p>
      <a href="{{context_path}}/reports/1/output/">Chellow</a> &gt;
      Gas Supplier Contracts
		 	[<a href="{{context_path}}/reports/401/output/">add</a>]
    </p>
    <br>
    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
      </ul>
    {% endif %}
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Start Date</th>
          <th>Finish Date</th>
        </tr>
      </thead>
      <tbody>
        {% for g_contract in contracts %}
          <tr>
            <td>
              <a href="{{context_path}}/reports/399/output/?g_contract_id={{g_contract.id}}">{{g_contract.name}}</a>
            </td>
            <td>{{g_contract.start_g_rate_script.start_date|hh_format}}</td>
            <td>{{g_contract.finish_g_rate_script.finish_date|hh_format}}</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </body>
</html>

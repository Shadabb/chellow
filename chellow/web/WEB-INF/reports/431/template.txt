<!doctype html>
<html>
  <head>
    <link rel="stylesheet" type="text/css"
      href="{{context_path}}/reports/19/output/">
    <title>
      Chellow &gt; Gas Contracts &gt; {{g_batch.g_contract.name}} &gt;
      Batches &gt; {{g_batch.reference}} &gt; Bill Imports
    </title>
  </head>
  <body>
    <p>
      <a href="{{context_path}}/reports/1/output/">Chellow</a> &gt;
      <a href="{{context_path}}/reports/395/output/">Gas Contracts</a> &gt;
      <a href="{{context_path}}/reports/399/output/?g_contract_id={{g_batch.g_contract.id}}">{{g_batch.g_contract.name}}</a> &gt;
      <a href="{{context_path}}/reports/413/output/?g_contract_id={{g_batch.g_contract.id}}">Batches</a> &gt;
      <a href="{{context_path}}/reports/417/output/?g_batch_id={{g_batch.id}}">{{g_batch.reference}}</a>
		 	&gt; Bill Imports
    </p>
    <br>
    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
      </ul>
    {% endif %}
    
    <form enctype="multipart/form-data" method="post" action=".">
      <fieldset>
        <input type="hidden" name="g_batch_id" value="{{g_batch.id}}">
        <legend>Import Bill File</legend>
        <input type="file" name="import_file" size="45"
          value="{{request.getParameter('import_file')}}">
        <input type="submit" value="Import">
        <br>
        <p>Valid file name extensions are {{parser_names}}.</p>
      </fieldset>
    </form>
    {% if importer_ids %}
      <ul>
        {% for id in importer_ids %}
          <li>
            <a href="{{context_path}}/reports/433/output/?importer_id={{id}}">{{id}}</a>
          </li>
        {% endfor %}
      </ul>
    {% endif %}
  </body>
</html>

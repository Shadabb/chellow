<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css"
      href="{{context_path}}/reports/19/output/" />
    <title>Chellow &gt; Gas Supplier Contracts &gt; {{contract.name}}</title>
  </head>
  <body>
    <p>
      <a href="{{context_path}}/reports/1/output/">Chellow</a> &gt;
      <a href="{{context_path}}/reports/395/output/">Gas Supplier Contracts</a>
		 	&gt; {{ contract.name }}
      [<a href="{{context_path}}/reports/403/output/?g_contract_id={{contract.id}}">edit</a>]
    </p>
    <br />
    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
      </ul>
    {% endif %}

    <table>
      <tbody>
        <tr>
          <th>Name</th>
          <td>{{contract.name}}</td>
        </tr>
        <tr>
          <th>Start Date</th>
          <td>{{contract.start_g_rate_script.start_date|hh_format}}</td>
        </tr>
        <tr>
          <th>Finish Date</th>
          <td>{{contract.finish_g_rate_script.finish_date|hh_format}}</td>
        </tr>
      </tbody>
    </table>
    <br />
    <table>
      <caption>
        Rate Scripts [<a href="{{context_path}}/reports/407/output/?g_contract_id={{contract.id}}">add</a>]
      </caption>
      <thead>
        <tr>
          <th></th>
          <th>From</th>
          <th>To</th>
        </tr>
      </thead>
      <tbody>
        {% for rate_script in rate_scripts %}
          <tr>
            <td>
              <a href="{{context_path}}/reports/411/output/?g_rate_script_id={{rate_script.id}}">view</a>
            </td>
            <td>{{rate_script.start_date|hh_format}}</td>
            <td>{{rate_script.finish_date|hh_format}}</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>

    <ul>
      <li>
        <a href="{{context_path}}/reports/413/output/?g_contract_id={{contract.id}}">Batches</a>
      </li>
    </ul>

    <h3>CSV Downloads</h3>

    <form action="{{context_path}}/reports/419/output/">
      <fieldset>
        <input
						type="hidden" name="g_contract_id" value="{{contract.id}}"/>
        <legend>Download Supply Level Virtual Bills</legend>
        <br/>
        From {{ input_date('start', month_start) }} to
        {{ input_date('finish', month_finish) }}
        <input type="submit" value="Download"/>
      </fieldset>
    </form>
    <br/>

    <h3>Script</h3>
    <pre>{{ contract.charge_script }}</pre>

    <h3 id="properties">Properties</h3>
    <pre>{{contract.properties}}</pre>
  </body>
</html>

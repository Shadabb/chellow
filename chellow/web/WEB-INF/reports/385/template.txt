<!doctype html>
<html>
  <head>
    <link rel="stylesheet" type="text/css"
      href="{{context_path}}/reports/19/output/">
    <title>
      Chellow &gt; Gas Supplies &gt; {{g_supply.id}} &gt; Edit
    </title>
  </head>
  <body>
    <p>
      <a href="{{context_path}}/reports/1/output/">Chellow</a> &gt;
      <a href="{{context_path}}/reports/437/output/">Gas Supplies</a> &gt;
      <a href="{{context_path}}/reports/423/output/?g_supply_id={{g_supply.id}}">{{g_supply.id}}</a> &gt;
      Edit
    </p>

    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{message}}</li>
        {% endfor %}
      </ul>
    {% endif %}

    <br>
    {% if request.getMethod() == 'GET' and request.getParameter('delete') %}
      <form method="post" action=".">
        <fieldset>
          <legend>Delete</legend>
          <input type="hidden" name="g_supply_id" value="{{g_supply.id}}">
          <p>Are you sure you want to delete this supply?</p>
          <input type="submit" name="delete" value="Delete">
        </fieldset>
      </form>

      <p>
        <a href="{{context_path}}/reports/423/output/?g_supply_id={{g_supply.id}}">Cancel</a>
      </p>
    {% else %}
      <form method="post" action=".">
        <fieldset>
          <legend>Update this supply</legend>
          <input type="hidden" name="g_supply_id" value="{{g_supply.id}}">
          <br>
          <label>Name</label> {{input_text('name', g_supply.name)}}
          <br>
          <label>MPRN</label> {{input_text('mprn', g_supply.mprn)}}
          <br>
          <br>
          <input type="submit" value="Update">
        </fieldset>
      </form>
      <br>
      <form action=".">
        <fieldset>
          <legend>Delete this supply</legend>
          <input type="hidden" name="g_supply_id" value="{{g_supply.id}}">
          <input type="submit" name="delete" value="Delete">
        </fieldset>
      </form>
      <br>
      <form method="post" action=".">
        <fieldset>
          <legend>Insert a new era</legend>
          <input type="hidden" name="g_supply_id" value="{{g_supply.id}}">
          <br>
          <label>Start date</label> {{input_date('start', None)}}
          <input type="submit" name="insert_g_era" value="Insert">
        </fieldset>
      </form>
      <br>
      <table>
        <caption>Existing Eras</caption>
        <thead>
          <tr>
            <th>Start date</th>
            <th>Finish date</th>
          </tr>
        </thead>
        <tbody>
          {% for g_era in g_eras %}
            <tr>
              <td>{{g_era.start_date|hh_format}}</td>
              <td>{{g_era.finish_date|hh_format}}</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    {% endif %}
  </body>
</html>
